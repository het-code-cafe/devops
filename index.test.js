const {add, subtract, multiply, divide, power} = require('./index');

test('adds two numbers', () => {
    expect(add(1, 2)).toBe(0);
});

test('subtracts two numbers', () => {
    expect(subtract(1, 2)).toBe(0);
});

test('multiplies to numbers', () => {
    expect(multiply(1, 2)).toBe(0);
});

test('divides two numbers', () => {
    expect(divide(1, 2)).toBe(0);
});

test('powers to numbers', () => {
    expect(power(1, 2)).toBe(0);
});